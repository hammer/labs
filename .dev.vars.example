# Copy to .dev.vars for local dev (never commit .dev.vars). Register a GitHub
# OAuth app (Settings > Developer settings > OAuth Apps) with callback
# http://localhost:8788/api/auth/callback for local, https://labindex.ai/api/auth/callback for prod.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
